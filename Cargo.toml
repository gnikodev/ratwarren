[package]
name = "ratwarren"
version = "0.1.0"
edition = "2024"
description = "A terminal UI client for SQL databases"
license = "MIT"
publish = false

[dependencies]
crossterm = "0.29"
ratatui = "0.30"
serde = { version = "1", features = ["derive"] }
toml = "1"
directories = "6"
thiserror = "2"

[dev-dependencies]
tempfile = "3"
